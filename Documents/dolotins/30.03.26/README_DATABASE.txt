-- Export of all users (passwords are already hashed)
-- To import this database:
-- 1. Start XAMPP and ensure MySQL is running
-- 2. Open phpMyAdmin (http://localhost/phpmyadmin)
-- 3. Click "Import" tab
-- 4. Choose this file and click "Go"

-- Or use command line:
-- mysql -u root -p < database.sql

-- Sample query to view all users (without passwords):
-- SELECT id, vards, uzvards, epasts, telefons, created_at FROM users;
